import MainLayout from '@/components/layout/MainLayout';
import CreatePollPageContent from '@/components/polls/CreatePollPageContent';

export default function CreatePollPage() {
  return (
    <MainLayout>
      <CreatePollPageContent />
    </MainLayout>
  );
}